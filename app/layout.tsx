import './globals.css'
import { Inter } from 'next/font/google'
import { Analytics } from '@vercel/analytics/react';

const inter = Inter({ subsets: ['latin'] })

export const metadata = {
  title: "Circuit GPT is an electrical engineer AI assistant to resolve queries.",
  description:
    "Circuit GPT is an electrical engineer AI assistant who has tons of experience in resolving queries related to substation equipment maintenance and its testing."
}

export default function RootLayout({
  children
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
